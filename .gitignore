node_modules/
dist/
shots/
src-tauri/target/
src-tauri/gen/schemas/
*.tmp
# Cliente OAuth do Google Cloud: repo publico, nunca versionar.
src-tauri/google-oauth.json
client_secret_*.json
# Updater signing key (minisign): lives in a GitHub secret, never in the repo.
*.key
test-results/
playwright-report/
